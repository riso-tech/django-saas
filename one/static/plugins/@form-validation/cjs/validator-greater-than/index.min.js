/**
 * FormValidation (https://formvalidation.io)
 * The best validation library for JavaScript
 * (c) 2013 - 2023 Nguyen Huu Phuoc <me@phuoc.ng>
 *
 * @license https://formvalidation.io/license
 * @package @form-validation/validator-greater-than
 * @version 2.0.2
 */

'use strict';
var e = require('@form-validation/core').utils.format;
exports.greaterThan = function () {
  return {
    validate: function (a) {
      if ('' === a.value) return { valid: !0 };
      var s = Object.assign({}, { inclusive: !0, message: '' }, a.options),
        n = parseFloat(''.concat(s.min).replace(',', '.'));
      return s.inclusive
        ? {
            message: e(
              a.l10n ? s.message || a.l10n.greaterThan.default : s.message,
              ''.concat(n),
            ),
            valid: parseFloat(a.value) >= n,
          }
        : {
            message: e(
              a.l10n ? s.message || a.l10n.greaterThan.notInclusive : s.message,
              ''.concat(n),
            ),
            valid: parseFloat(a.value) > n,
          };
    },
  };
};
