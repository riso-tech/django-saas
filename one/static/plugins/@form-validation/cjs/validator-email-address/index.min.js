/**
 * FormValidation (https://formvalidation.io)
 * The best validation library for JavaScript
 * (c) 2013 - 2023 Nguyen Huu Phuoc <me@phuoc.ng>
 *
 * @license https://formvalidation.io/license
 * @package @form-validation/validator-email-address
 * @version 2.0.2
 */

'use strict';
var a =
    /^(([^<>()[\]\\.,;:\s@"]+(\.[^<>()[\]\\.,;:\s@"]+)*)|(".+"))@[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?(?:\.[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?)*$/,
  e =
    /^(([^<>()[\]\\.,;:\s@"]+(\.[^<>()[\]\\.,;:\s@"]+)*)|(".+"))@[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?(?:\.[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?)+$/;
exports.emailAddress = function () {
  return {
    validate: function (r) {
      if ('' === r.value) return { valid: !0 };
      var t = Object.assign(
          {},
          { multiple: !1, requireGlobalDomain: !1, separator: /[,;]/ },
          r.options,
        ),
        i = t.requireGlobalDomain ? e : a;
      if (!0 === t.multiple || 'true' === ''.concat(t.multiple)) {
        for (
          var l = t.separator || /[,;]/,
            s = (function (a, e) {
              for (
                var r = a.split(/"/), t = r.length, i = [], l = '', s = 0;
                s < t;
                s++
              )
                if (s % 2 == 0) {
                  var u = r[s].split(e),
                    n = u.length;
                  if (1 === n) l += u[0];
                  else {
                    i.push(l + u[0]);
                    for (var o = 1; o < n - 1; o++) i.push(u[o]);
                    l = u[n - 1];
                  }
                } else (l += '"' + r[s]), s < t - 1 && (l += '"');
              return i.push(l), i;
            })(r.value, l),
            u = s.length,
            n = 0;
          n < u;
          n++
        )
          if (!i.test(s[n])) return { valid: !1 };
        return { valid: !0 };
      }
      return { valid: i.test(r.value) };
    },
  };
};
