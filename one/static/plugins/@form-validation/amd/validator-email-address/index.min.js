/**
 * FormValidation (https://formvalidation.io)
 * The best validation library for JavaScript
 * (c) 2013 - 2023 Nguyen Huu Phuoc <me@phuoc.ng>
 *
 * @license https://formvalidation.io/license
 * @package @form-validation/validator-email-address
 * @version 2.0.2
 */

define(['exports'], function (a) {
  'use strict';
  var e =
      /^(([^<>()[\]\\.,;:\s@"]+(\.[^<>()[\]\\.,;:\s@"]+)*)|(".+"))@[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?(?:\.[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?)*$/,
    r =
      /^(([^<>()[\]\\.,;:\s@"]+(\.[^<>()[\]\\.,;:\s@"]+)*)|(".+"))@[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?(?:\.[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?)+$/;
  a.emailAddress = function () {
    return {
      validate: function (a) {
        if ('' === a.value) return { valid: !0 };
        var t = Object.assign(
            {},
            { multiple: !1, requireGlobalDomain: !1, separator: /[,;]/ },
            a.options,
          ),
          i = t.requireGlobalDomain ? r : e;
        if (!0 === t.multiple || 'true' === ''.concat(t.multiple)) {
          for (
            var l = t.separator || /[,;]/,
              n = (function (a, e) {
                for (
                  var r = a.split(/"/), t = r.length, i = [], l = '', n = 0;
                  n < t;
                  n++
                )
                  if (n % 2 == 0) {
                    var s = r[n].split(e),
                      u = s.length;
                    if (1 === u) l += s[0];
                    else {
                      i.push(l + s[0]);
                      for (var o = 1; o < u - 1; o++) i.push(s[o]);
                      l = s[u - 1];
                    }
                  } else (l += '"' + r[n]), n < t - 1 && (l += '"');
                return i.push(l), i;
              })(a.value, l),
              s = n.length,
              u = 0;
            u < s;
            u++
          )
            if (!i.test(n[u])) return { valid: !1 };
          return { valid: !0 };
        }
        return { valid: i.test(a.value) };
      },
    };
  };
});
